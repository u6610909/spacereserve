# TESTING ONLY — shows the per-role quick-login buttons on the sign-in page of
# the production build. The backend must also run with ALLOW_DEV_LOGIN=true for
# them to work. Set this to false (and rebuild) before the graded submission
# and the demo recording.
VITE_ALLOW_DEV_LOGIN=true
